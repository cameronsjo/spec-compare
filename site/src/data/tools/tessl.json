{
  "tool": "tessl",
  "displayName": "Tessl",
  "vendor": "Tessl",
  "website": "https://tessl.io",
  "version": "Framework (closed beta)",
  "license": "Proprietary",
  "maturity": "Beta",
  "tier": "core",
  "philosophy": "context",
  "specMaturity": "Spec-as-Source",
  "tagline": "Spec-as-source: humans edit specs, code generates.",
  "bestFor": "Spec-as-source",
  "complexity": "High",
  "learningCurve": "Moderate",
  "features": {
    "gitWorktrees": false,
    "multiAgent": false,
    "dashboard": "No",
    "mcp": true,
    "ideIntegration": false,
    "openSource": false,
    "parallel": false
  },
  "agentConfig": { "agentsMd": "no", "claudeMd": "no", "skillMd": "no", "slashCommands": 0 },
  "phases": [
    { "id": "registry", "label": "Spec Registry", "kind": "govern", "note": "10,000+ specs as installable dependencies — context that prevents API hallucinations." },
    { "id": "spec", "label": "Author spec", "kind": "specify", "note": "Author or edit the spec — the primary maintained artifact." },
    { "id": "generate", "label": "Generate code", "kind": "implement", "note": "Framework generates and auto-syncs code, marked as disposable." },
    { "id": "verify", "label": "Verify", "kind": "review", "note": "Hard guardrails enforced via tests." }
  ],
  "edges": [
    { "from": "registry", "to": "spec" },
    { "from": "spec", "to": "generate" },
    { "from": "generate", "to": "verify" },
    { "from": "verify", "to": "spec", "on": "regenerate" }
  ],
  "scenarios": [
    { "id": "trivial-mod", "steps": ["spec", "generate"], "score": 4, "note": "Edit the spec directly, regenerate code. **Elegant** — but closed beta limits access." },
    { "id": "greenfield", "steps": ["registry", "spec", "generate", "verify"], "score": 3, "note": "Spec-as-source works, but **one-to-one mapping may limit architecture**. Closed beta." },
    { "id": "refactor", "steps": ["spec", "generate", "verify"], "score": 4, "note": "Edit spec, regenerate implementation. Works well theoretically." },
    { "id": "bug-fix", "steps": ["spec", "generate"], "score": 3, "note": "Edit spec, regenerate. Fast but **unproven in emergencies**." },
    { "id": "parallel-dev", "steps": ["spec", "generate"], "score": 2, "note": "No parallel feature support mentioned." },
    { "id": "cross-cutting", "steps": ["registry", "spec", "generate", "verify"], "score": 3, "note": "Edit multiple specs, regenerate — but manual coordination." }
  ],
  "scores": { "trivial": 4, "medium": 4, "large": 3, "parallel": 2, "emergency": 1, "solo": 2, "contextMgmt": 2, "overall": 2.6 },
  "keyFeatures": [
    "Spec-as-source: humans edit specs, code generates and auto-syncs",
    "Spec Registry — 10,000+ specs as installable dependencies",
    "Hard guardrails enforced via tests",
    "Model-agnostic through MCP",
    "One-to-one spec ↔ code file mapping"
  ],
  "limitations": [
    "Framework still in closed beta (limited access)",
    "Most experimental / unproven approach",
    "One-to-one spec-to-code mapping may not scale",
    "Vendor lock-in; unknown pricing model",
    "Requires a significant mindset shift (specs as source)"
  ]
}
