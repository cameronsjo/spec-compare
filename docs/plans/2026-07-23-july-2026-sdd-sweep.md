# July 2026 SDD Sweep — Five New Emerging Entries + Spec Kitty Refresh

**Panel:** reviewed by plan-reviewer + red-team seats (Sonnet); 2 blockers, 4 majors, 2 high live-state refutations found and folded in below.

## Context

Cameron flagged six items: five new SDD frameworks (moia-adk, grace, gaii, frame, smart-ralph) and a spec-kitty update. Research (GitHub-only sourcing, per Cameron) identified all six; Cameron confirmed the triage verdicts in-session:

- **moai-adk** (Cameron's "moia-adk" was a transposition) → emerging entry now, with a stated core-promotion trigger
- **Frame, GRACE, GAAI, smart-ralph** → emerging entries
- **GAAI** = `Fr-e-d/GAAI-framework` — Cameron confirmed the phonetic "gaii" match is the right project
- **spec-kitty** → refresh the existing core entry (v3.1.9 → v3.2.5)

This mirrors the May 2026 sweep (`docs/reassessment-2026-05-31.md`), scoped narrower: no full re-score of existing tools, but a cheap version-drift spot-check justifies bumping the global assessment date.

## Verdict summary (red-team-verified 2026-07-23)

| Tool | Repo | Version | License | Stars | maturity (literal enum value) |
|---|---|---|---|---|---|
| MoAI-ADK | `modu-ai/moai-adk` | v3.0.0 (2026-07-19) | Apache-2.0 | ~1.1k | `Active Dev` |
| Frame | `kaanozhan/Frame` | **v2.4.0 (2026-06-25)** | Apache-2.0 | ~318 | `Active Dev` |
| GRACE | `osovv/grace-marketplace` | v4.0.0-rc.3 (2026-07-22) | MIT | ~227 | `Beta` (all-rc caveat goes in limitations, NOT in this field) |
| GAAI | `Fr-e-d/GAAI-framework` | **v2.49.0** (git tags only; no GitHub Releases) | Elastic License v2 | ~154 | `Active Dev` (30+ tags, ~1.9k commits, pushed daily — not Preview) |
| Smart Ralph | `tzachbon/smart-ralph` | v4.0.0 (2026-02-20) | MIT | ~430 | `Active Dev` |

**Version double-source rule (red-team finding):** GitHub's `releases/latest` and `/tags` endpoints disagreed on Frame (`/releases?per_page=10` never surfaced v2.4.0). For every version written this pass — the five new tools AND the Part-A spot-check — pull BOTH `gh api repos/<o>/<r>/releases/latest` and `/tags`, take the newer. GAAI's license: source from the LICENSE file, not `gh api .../license` (GitHub reports `NOASSERTION` for ELv2 — the API will second-guess a correct call).

**Smart Ralph pulse (resolved live by red-team):** latest commit 2026-07-23, prior 2026-04-08. Cameron's demotion condition ("commits also stalled since Feb") does NOT fire → emerging entry stands. Record the sparse cadence (~2 commits Mar–Jul 2026) in its limitations and in sweep-doc Part B. No further gating at execution.

### One-line identities (source for taglines/keyFeatures/bestFor)

- **MoAI-ADK**: Korean-origin (modu-ai) Go CLI wrapping Claude Code in a SPEC-First Plan→Run→Sync lifecycle — TDD gates (85%+ coverage), 24 agents/52 skills, per-SPEC git worktrees, "Tokenomics" model/effort routing. Claude-Code-only.
- **Frame**: Electron "Agentic Development Environment" — spec→plan→tasks→outcome Markdown per feature, multi-agent orchestrator running Claude Code/Codex CLI/Gemini CLI in parallel isolated worktrees with footprint-conflict + drift detection. Solo maintainer.
- **GRACE** (Graph-RAG Anchored Code Engineering): contract-first methodology as installable agent skills — XML artifacts (requirements/technology/development-plan/verification-plan/knowledge-graph), Initialize→Plan→Verify→Execute→Refresh, drift detection. Claude Code/Codex/Kilo Code; Bun CLI. Solo maintainer.
- **GAAI** (Governed Agentic AI Infrastructure): conversational Discovery phase → Epics/Stories backlog (git-tracked contract) → autonomous Delivery daemon in isolated Claude Code/tmux sessions with QA gates + persistent cross-session memory. Markdown+YAML+bash, no SDK. Successor to AI-Governor-Framework. ELv2 = source-available, not OSI open source.
- **Smart Ralph**: Claude Code/Codex plugin layering spec phases (research→requirements→design→tasks→implement, state files, phase-dedicated sub-agents) on the Ralph autonomous loop; fresh context per task; alternate "ralph-speckit" flavor mirrors spec-kit phases.

## Files to change

### 1. Five new tool JSONs — `site/src/data/tools/<slug>.json`

Slugs: `moai-adk`, `frame`, `grace`, `gaai`, `smart-ralph`. All `tier: "emerging"` — schema forbids `phases`/`edges`/`scenarios` on emerging (validator enforces).

**Calibrate against actual emerging-tier entries** — find them with `grep -l '"tier": "emerging"' site/src/data/tools/*.json` and match their field set, score norms, and optional-field altitude. Do NOT calibrate against `superpowers.json`/`traycer.json` — panel confirmed both are **core** tier (promoted May 2026) and pattern-matching them imports exactly the fields emerging forbids.

Required fields per `schema.json`: tool, displayName, vendor, license, maturity, tier, tagline, bestFor, complexity, learningCurve, version, features (all seven flags), scores, keyFeatures, limitations. Complete value table (verify flags marked ⚠ against the repo README via `gh api repos/<o>/<r>/readme`):

| Field | moai-adk | frame | grace | gaai | smart-ralph |
|---|---|---|---|---|---|
| vendor | modu-ai | kaanozhan (solo) | osovv (solo) | Fr-e-d (solo) | tzachbon (solo) |
| philosophy | agent | agent | constitution | constitution | loop |
| specMaturity | Spec-First | Spec-First | Spec-Anchored | Spec-First | Spec-First |
| complexity | High | Medium | High | Medium | Medium |
| learningCurve | Steep | Moderate | Steep | Moderate | Moderate |
| bestFor | Disciplined TDD with Claude Code | GUI-orchestrated parallel agents | Contract-first formal specs | Governed autonomous delivery | Hands-off spec-to-code loops |
| features.gitWorktrees | true | true | ⚠ | ⚠ (tmux-session isolation ≠ worktrees) | ⚠ |
| features.multiAgent | true | true | ⚠ | true | true |
| features.parallel | true | true | false | ⚠ | "limited" ([P] tasks) |
| features.dashboard | ⚠ ("None" unless README shows one) | "Desktop GUI" | "None" | ⚠ (Delivery daemon may have status UI) | "None" |
| features.mcp | ⚠ | ⚠ | ⚠ | ⚠ | ⚠ (default false if README silent) |
| features.ideIntegration | false | false (own desktop app) | false | false | false |
| features.openSource | true | true | true | **false** (ELv2; explain in limitations) | true |
| scores t/m/l/p/e/s/c | 2/4/4/4/1/3/4 | 3/3/3/4/2/3/3 | 1/3/4/2/1/2/3 | 2/3/4/3/1/2/4 | 2/3/4/3/1/3/4 |
| scores.overall (explicit — do not re-derive) | **3.1** | **3.0** | **2.3** | **2.7** | **2.9** |

Overall values above are the unweighted mean of the seven scores rounded to one decimal (fixed editorially — write as given). learningCurve/complexity vocabulary: match the exact strings existing entries use; if the emerging precedent uses different casing/terms, theirs wins.

**⚠-flag resolution guide:** `gitWorktrees` = true only if the README names git worktrees as a mechanism; `multiAgent` = true for orchestrated multiple agents, `"limited"` for sub-agent delegation inside one session; `parallel` = true only for concurrent independent workstreams; `mcp` = true only on explicit MCP support. README silent → false. When ambiguous, false + a keyFeatures/limitations note beats optimistic true.

Limitations must carry the honest caveats: solo-maintainer (frame, grace, gaai, smart-ralph), Claude-Code-only + self-reported benchmarks (moai-adk), all-rc versioning + Bun dependency (grace), ELv2 + no GitHub Releases + macOS-only-validated (gaai), sparse commit cadence ~2 commits Mar–Jul 2026 (smart-ralph).

No `docs/tools/<slug>.md` profiles — those are core-tier only.

### 2. Spec Kitty refresh — `site/src/data/tools/spec-kitty.json` + `docs/tools/spec-kitty.md`

- `version`: v3.1.9 → **v3.2.5** (2026-07-08)
- keyFeatures: add charter consolidation (`charter.yaml`, was 4 files), append-only event-log work-package state (phase-1 dual write), coordination-topology/split-brain fixes, new CLI surfaces (`review --check-residual`, `orchestrator-api resolve-workspace`, `doctor shim-registry`)
- limitations: note breaking-change cadence (`--feature` alias removed from 8 commands, `auth whoami` removed, pre-3.2 missions without `meta.json` unsupported)
- Traction: 1,443★ / 128 forks / 482 open issues — very active, multi-contributor, ADR/RFC-governed (red-team verified exact)
- Scores/phases/scenarios: **unchanged** (no hands-on re-eval this pass; note that in the sweep doc)
- Profile doc: add a "July 2026" update paragraph covering the above

### 3. Sweep doc — `docs/reassessment-2026-07-23.md` (new)

Mirror `docs/reassessment-2026-05-31.md` structure:
- Part A — version drift: spec-kitty (detailed, above) + spot-check the other 12 tracked tools using the **double-source rule** (`releases/latest` + `/tags`, take newer). Version strings only; no re-scoring. Large drift on a core tool becomes a follow-up issue, not scope creep here.
- Part B — corrections: record the smart-ralph pulse-check result (2026-07-23 + 2026-04-08 commits, gate not fired) and the Frame/GAAI version corrections the red-team made.
- Part C — new-tool verdicts: the five above, each with evidence links and triage rationale. **MoAI-ADK carries an explicit core-promotion trigger**: hands-on eval of the v3.0 Plan→Run→Sync lifecycle (install, run a SPEC through all three phases, score the six scenarios) — file a follow-up issue for it.
- Part D — citations (GitHub links from research).
- Identification-confidence notes: "moia-adk" → moai-adk transposition; "gaii" → GAAI phonetic match, Cameron-confirmed; GRACE sole-candidate match.

### 4. Housekeeping

- `docs/gaps.md`: add a July 2026 section noting the five additions (and the GAAI ID caveat); update the "reassessed" date line. (No demotions this pass — smart-ralph's gate didn't fire.)
- `docs/tools/ralph-loop.md:74`: smart-ralph ecosystem one-liner → note it's now tracked as its own entry.
- `site/src/types.ts`: `ASSESSED_AS_OF` → `'2026-07-23'` (justified by the Part-A spot-check).
- `README.md`: "Last Updated" AND the hardcoded tool count — "all thirteen tools" becomes **eighteen**. Grep README + docs for other "thirteen"/"13 tools" instances.
- `CHANGELOG.md` entry (`feat:` — conventional commits); follow the May precedent's count convention: "18 tools, 8 core".
- Regenerate: `npm run gen:tables` (rewrites the three GEN blocks in `docs/comparison.md`).

## Verification

**All npm commands run from `site/`** (package.json lives there; repo root gives ENOENT, exit 254).

1. `cd site && npm run validate` — AJV schema + cross-tool invariants. Capture real exit code, never pipeline-tail.
2. `cd site && npm run gen:check` — generated tables not stale.
3. `cd site && npm run build` — full site build green.
4. Spot-render: `cd site && npm run dev`, confirm the five new cards + heatmap rows appear and spec-kitty shows v3.2.5.

## Execution logistics

- Branch-mode repo, primary checkout: worktree before any file mutation (entry posture: push -u + draft PR at entry). Branch `update/2026-07-sweep`.
- Ignore the stale `origin/claude/*` remote branches — pre-schema-era forks; don't merge or rebase onto them.
- Commit per unit (tool JSONs batch as one `feat:` commit; spec-kitty refresh, sweep doc, housekeeping separate).
- Polish before PR-finalization; **CodeRabbit is credit-exhausted** — self-review substitutes; don't wait on CodeRabbit.
- PR body: cite sources per candidate, redaction pass before posting. Follow-up issues: MoAI-ADK core-promotion eval; any large drift found in Part A.

## Alternatives declined

- **Core entry for moai-adk this pass** — declined (Cameron): README-depth research can't honestly score six scenarios; emerging-now with a hands-on promotion trigger instead.
- **Watch-list for GAAI** — declined (Cameron confirmed the identification); ELv2/no-GitHub-Releases caveats carried in limitations instead.
- **Full 13-tool re-score** — out of scope; version spot-check only, matching the ask.
